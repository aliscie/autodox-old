type FileMode = variant {
    Private;
    Public;
    Restricted
};

type CreateFileData = record {
    content : text;
    mode : FileMode;
    name : text;
    parent_id : text;
};

service : {
    "get_files": (CreateFileData) -> (vec text) query;
    "create_file": (entry: text) ->();
}

